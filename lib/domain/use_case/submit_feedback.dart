/*
class SubmitFeedback {
  final FeedbackRepository repository;

  SubmitFeedback(this.repository);

  Future<void> call(FeedbackModel feedback) async {
    await repository.submitFeedback(feedback);
  }
}
*/